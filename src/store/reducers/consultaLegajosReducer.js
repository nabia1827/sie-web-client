import { types } from "../types";

const initialValues = {
    
};

export const consultaLegajoReducer = (state = initialValues, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
};